

cd ~/OneDrive/Desktop/Code

# this gives your system access and deletes these folders while printing them out in the terminal, your can play with this, so it prints to a file or the window persists, etc

chmod u+x delete_node_modules.sh
find . -name 'node_modules' -type d -prune -print -exec rm -rf '{}' \;

# during development, use the $SHELL command if you want the window to stay open, so you can see the output or errors

# $SHELL